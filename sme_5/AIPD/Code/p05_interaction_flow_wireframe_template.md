# Interaction Flow & Wireframe Template — <Your Product>

> Reusable blank for [P05](../writeups/P05_interaction_flow_wireframe.md). Build one interaction flow + 4 wireframes + the AI-UI checklist.

## 1. Interaction flow (mermaid or ASCII)
Every branch must end at a screen. Include an "honest fallback" branch for when the AI can't answer.

```
[user intent] ──► [AI/system action] ──► [screen shown] ──► [feedback recorded]
```

## 2. Wireframe W1 — <Screen name> (e.g., Home)
```
┌────────────────────────┐
│                        │
│                        │
│                        │
│                        │
│                        │
│                        │
│                        │
└────────────────────────┘
```

## 3. Wireframe W2 — <Screen name>
```
┌────────────────────────┐
│                        │
│                        │
│                        │
│                        │
│                        │
│                        │
│                        │
└────────────────────────┘
```

## 4. Wireframe W3 — <Screen name>
```
┌────────────────────────┐
│                        │
│                        │
│                        │
│                        │
│                        │
│                        │
│                        │
└────────────────────────┘
```

## 5. Wireframe W4 — <Screen name>
```
┌────────────────────────┐
│                        │
│                        │
│                        │
│                        │
│                        │
│                        │
│                        │
└────────────────────────┘
```

> Wireframe conventions: `[ ]` = button, `▸` = navigable row, `text…` = input field. Boxes only — no colour/art.

## 6. AI-UI rule checklist (from Unit 2.6–2.7)
| Rule | Where MY product applies it |
|---|---|
| Set expectations before the AI acts (e.g., "~30 s") | |
| Ground outputs in user's data (citations) | |
| Show confidence / admit limits (honest fallback) | |
| Let the user steer & correct (ratings, retry) | |
| Explain what the system did (explainability) | |
